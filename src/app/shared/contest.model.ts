export class Contest {

    constructor(public name: string, public category: string, public posts: number, public id: number, public userId: number, public description: string, public imageAddress: string) { }
}